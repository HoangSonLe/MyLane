/**
 * Plain-JS mirror of src/services/home/home.mock.ts — keep both in sync.
 */
export const MOCK_LAST_PLAYED = {
  game: 'Sequence Memory',
  mode: 'Classic',
  score: 11,
  maxScore: 20,
  roundsPlayed: 47,
}
